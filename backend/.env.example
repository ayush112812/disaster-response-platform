# Server
PORT=5000
NODE_ENV=development

# Supabase
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Google Gemini API (for location extraction and image verification)
GEMINI_API_KEY=your_gemini_api_key

# Mapbox (for geocoding)
MAPBOX_ACCESS_TOKEN=your_mapbox_access_token

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX=100  # 100 requests per window

# CORS
CORS_ORIGIN=http://localhost:3000 Configuration
PORT=5000
NODE_ENV=development

# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_KEY=your_supabase_anon_key
SUPABASE_SERVICE_KEY=your_supabase_service_role_key  # For admin operations

# Google AI (Gemini) Configuration
GOOGLE_AI_KEY=your_google_ai_api_key

# Mapbox Configuration (for geocoding)
MAPBOX_ACCESS_TOKEN=your_mapbox_access_token

# Twitter API (optional, will use mock data if not provided)
TWITTER_BEARER_TOKEN=your_twitter_bearer_token

# Caching
CACHE_TTL=3600  # 1 hour in seconds

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100  # Max requests per window per IP

# Logging
LOG_LEVEL=info  # error, warn, info, debug

# WebSocket
WS_PATH=/api/socket.io/  # WebSocket path

# Security
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=7d

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000
